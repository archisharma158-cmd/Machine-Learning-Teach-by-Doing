LECTURE 01 NOTES :
..................................................................................................

1.import important libraries .

following are the libraries imported and their use and purpose have been listed along with them.

a). Pandas
b). Numpy
c). Matplotlib
d). Random.seed(0)

Keywords Uswd And Their Functions :

random.seed(0)
np.random.normal(loc=5, scale=1, size=10)
np.vstack((dogs_whisker_length, dogs_ear_flappiness_index)).T
np.hstack
train_test_split
plt.scatter
plt.xlabel
plt.ylabel
plt.title
plt.lagend
plt.show()
